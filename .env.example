# Convex deployment URL. Get one by running `npx convex dev` in this repo —
# it provisions a deployment and writes the value into .env.local for you.
#
# Public by design: the browser needs it to reach Convex. Nothing secret
# belongs in a NEXT_PUBLIC_ variable.
NEXT_PUBLIC_CONVEX_URL=
